{
  "name": "3d-chess",
  "version": "0.1.0",
  "private": true,
  "description": "Chess in a cube — a 3D chess game served as a static GitHub Pages site.",
  "type": "module",
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "serve": "python3 -m http.server 8000"
  }
}
